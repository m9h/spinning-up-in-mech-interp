# Runnable rungs use only these — install and run any of rungs 1-5 as-is.
torch>=2.2
numpy
transformers>=4.44        # rungs 2, 4, 5 (GPT-2, plain HF, no TransformerLens needed)
huggingface_hub           # rung 5 (downloads the GPT-2 SAE tensors)
safetensors               # rung 5
torchvision               # rung 1 (InceptionV1 = googlenet)

# Optional but recommended: free, local-first experiment tracking (drop-in for wandb).
# Every rung produces numbers worth comparing across runs -- see GETTING_STARTED.md.
# trackio

# --- Canonical tooling (optional): the source papers use these; the rungs point to them ---
# transformer_lens        # rungs 2, 4 — cleaner per-head work; ARENA uses it
# sae_lens                # rung 5 — pretrained SAEs incl. Gemma Scope (one-line release swap)
# lucent                  # rung 1 — optimization-based feature visualization (OpenAI Microscope)
# circuit-tracer          # rung 7 — github.com/safety-research/circuit-tracer

# --- Rungs 6, 8 — lenses + the recent-claim capstones (our companion code) ---
# git+https://github.com/anthropics/jacobian-lens.git
# git+https://github.com/m9h/jlens-lab.git

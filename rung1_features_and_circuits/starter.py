"""Rung 1 starter — feature visualization + the randomization control (InceptionV1).
Needs: pip install torch lucent torchvision.  Fill the TODOs; the control is the point."""
# TODO: load InceptionV1 (via lucent.modelzoo) and pick a mid-layer neuron (e.g. mixed3b:379)
# TODO: feature-visualize it (lucent.optvis.render) + show top-activating ImageNet crops
# CONTROL: re-initialize the model weights and re-run the visualization. A real feature
#          collapses to noise on a randomized net (Adebayo et al. 2018 sanity check).
print("See README.md — implement the load + optvis, then run the randomized-net control.")
